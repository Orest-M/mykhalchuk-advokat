# Copy to .dev.vars for local `wrangler dev` / `astro dev` secret access via
# Astro.locals.runtime.env. Never commit .dev.vars. Values are placeholders.
# Production values are set with `wrangler secret put <NAME>`.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TURNSTILE_SECRET_KEY=
IP_HASH_SALT=
LEADS_EXPORT_TOKEN=
